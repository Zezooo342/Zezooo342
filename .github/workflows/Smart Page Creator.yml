on:
  schedule:
    # تشغيل كل يوم الساعة 9 صباحاً
    - cron: '0 9 * * *'
  workflow_dispatch:
    # تشغيل يدوي
    inputs:
      topics:
        description: 'المواضيع (مفصولة بفواصل)'
        required: false
        default: 'الذكاء الاصطناعي,البرمجة,التسويق'

jobs:
  create-pages:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v3

    - name: 🐍 Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: 📦 Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: 🤖 Run Smart Creator
      env:
        PERPLEXITY_API_KEY: ${{ secrets.PERPLEXITY_API_KEY }}
      run: |
        python smart_page_creator.py

    - name: 📤 Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      if: github.ref == 'refs/heads/main'
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./smart_pages

    - name: 💾 Commit results
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add smart_pages/
        git diff --staged --quiet || git commit -m "🤖 Auto-generated pages $(date)"
        git push
